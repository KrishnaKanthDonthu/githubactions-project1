name: push_workflow
on: 
    push:
        branches:
        - main
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: first step
              run: echo "this is my first workflow"
            - name: print good bye
              run: |
                echo "this is my first workflow"
                echo "Workflow execued successfully"